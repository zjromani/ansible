# Opt-in only. Default local.yml / update.yml skip this unless:
#   -e enable_cursor_mcp=true
- name: Expand Cursor MCP config via setup-mcps
  shell: bash {{ playbook_dir }}/scripts/setup-mcps.sh
  args:
    executable: /bin/zsh
  tags:
    - cursor-mcp
    - setup-mcps
