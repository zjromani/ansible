- name: Load vault secrets
  include_vars: "{{ playbook_dir }}/vars/secrets.yml"
  tags: secrets

- name: Write ~/.zshenv_private from vault
  copy:
    dest: "{{ ansible_env.HOME }}/.zshenv_private"
    mode: '0600'
    content: "{{ zshenv_private }}"
  tags: secrets
